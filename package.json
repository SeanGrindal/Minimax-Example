{
  "name": "tic-tact-toe-ai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=deploy --cleanup"
  },
  "dependencies": {
    "@nuxtjs/style-resources": "^1.0.0",
    "brain.js": "^2.0.0-beta.2",
    "core-js": "^3.8.3",
    "node-sass": "^4.14.1",
    "nuxt": "^2.14.12",
    "sass-loader": "^9.0.3"
  },
  "devDependencies": {
    "push-dir": "^0.4.1"
  }
}
